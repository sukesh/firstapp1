<!DOCTYPE>
<html>
<head>
<title><%=@title%></title>
<%= csrf_meta_tag %>
<%= render 'layouts/stylesheet' %>
</head>
<body>
<div class="container">
<%= render 'layouts/header' %>
<section class="round">
<%= yield %>
</section>
<%= render 'layouts/footer' %><br><br><br><br><br>
<b><div align="center"><u>Debug</u><font color="red">
<%= debug(params) if !Rails.env.develpment? %>
</b></font>
</div>
</div>
</body>
</html>
